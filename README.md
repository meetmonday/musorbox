# MusorBox

Учебный мобильный портал в стиле начала 2010-х: новости, статьи, обзоры, форум,
комментарии, голосования и профили пользователей.

Проект написан с нуля на Bun, Hono и Drizzle ORM и наполнен демонстрационным
контентом. Внешний вид намеренно повторяет эстетику мобильных порталов 2013 года.

## Возможности

- Лента новостей с категориями и пагинацией
- Поиск по заголовкам и текстам открытых топиков, включая кириллицу без учёта регистра
- Форумы с тегами, вложенными комментариями и AJAX-ответами
- Регистрация, логин/логаут и сессии на bcrypt
- Голосование за топики и комментарии
- Редактор топиков с загрузкой изображений
- Карусель «Интересное», сайдбар, рейтинги авторов
- RSS-ленты (`/feed_topics/:channel`)

Поиск доступен из шапки и по адресу `/public/search/?string=запрос`.
Ищется буквальное вхождение фразы: `%` и `_` не являются шаблонами.
Скрытые топики не выдаются; пустой запрос предлагает ввести текст, а не показывает всю ленту.
Выдача использует обычные карточки и пагинацию портала (`PAGE_SIZE`), сохраняя запрос
при переходах. Поиск последовательно читает открытые топики без отдельного индекса;
это позволяет учитывать регистр кириллицы без расширений SQLite и изменений схемы.

## Стек

- [Bun](https://bun.com) — рантайм
- [Hono](https://hono.dev) — веб-фреймворк (JSX)
- [Drizzle ORM](https://orm.drizzle.team) + bun:sqlite
- [bcryptjs](https://github.com/dcodeIO/bcrypt.js) — хеширование паролей
- TypeScript (strict)

## Быстрый старт

```bash
bun install
bun run seed    # опционально: наполнить демо-контентом
bun run dev     # http://localhost:3000
```

Схема базы создаётся автоматически при первом запуске (миграции применяются
на старте из папки `drizzle/`).

## Демо-аккаунты

| Логин  | Пароль   | Роль    |
| ------ | -------- | ------- |
| admin  | secret   | editor  |
| demo   | demo123  | author  |
| Helper | secret   | author  |
| Reader | secret   | user    |

## Скрипты

| Команда                  | Описание                              |
| ------------------------ | ------------------------------------- |
| `bun run dev`            | Запуск с автоперезагрузкой            |
| `bun run start`          | Запуск без автоперезагрузки           |
| `bun run db:generate`    | Сгенерировать миграцию                |
| `bun run db:migrate`     | Применить миграции вручную            |
| `bun run db:push`        | Синхронизировать схему с БД           |
| `bun run db:studio`      | Открыть Drizzle Studio                |
| `bun run typecheck`      | Проверка типов                        |
| `bun run seed`           | Наполнить демо-данными                |

## Переменные окружения

| Переменная         | По умолчанию      | Описание                    |
| ------------------ | ----------------- | --------------------------- |
| `PORT`             | `3000`            | Порт HTTP-сервера           |
| `DB_PATH`          | `./musorbox.db`   | Путь к файлу SQLite         |
| `SESSION_SECRET`   | `dev-secret-...`  | Секрет сессий (сменить!)    |
| `PAGE_SIZE`        | `20`              | Топиков на страницу         |
| `MAIN_PAGE_SIZE`   | `10`              | Топиков на главной          |

## Структура

```
src/
  app.tsx          # точка входа, маршрутизация
  auth/            # логин/регистрация/сессии
  core/            # config, db, schema, seed, утилиты
  topics/          # лента, топики, обзоры
  forum/           # форум, теги, обсуждения
  comments/        # комментарии
  votes/           # голосования
  users/           # профили и рейтинги
  editor/          # создание топиков и загрузка файлов
  pages/           # статичные страницы и RSS
  layout/          # шапка, подвал, каркас
  sidebar/         # виджеты сайдбара
```

## Запуск в Docker (с Cloudflare Tunnel)

Приложение и cloudflared живут в одном контейнере; база и статика —
снаружи (volumes). Туннель поднимается только если задан `TUNNEL_TOKEN`.

### Первый запуск

```bash
cp .env.example .env
# заполните в .env: TUNNEL_TOKEN, PUBLIC_BASE_URL=https://your.domain, SESSION_SECRET

mkdir -p data
# опционально: перенести существующую БД (сначала остановить локальный сервер)
# cp musorbox.db data/

docker compose up -d --build
docker compose ps        # контейнер должен быть (healthy)
curl http://localhost:3000
```

Если `public/` ещё не лежит на хосте, положите ассеты в `./public`
(в образ они не копируются, так как заигнорены в git).

### Как это устроено

- `DB_PATH=/data/musorbox.db`, каталог `./data` смонтирован в `/data`;
  миграции применяются на старте автоматически.
- `./public` смонтирован в `/app/public` (чтение/запись — редактор пишет
  `public/uploads/`).
- `TUNNEL_TOKEN` → `cloudflared tunnel run --token ...` внутри контейнера,
  процесс супервизируется `docker/entrypoint.sh`.
- `PORT` на хосте открыт только для локальной отладки — туннелю он не нужен.

### Как получить токен туннеля

Cloudflare Zero Trust → Networks → Tunnels → создать/выбрать туннель →
Configure → «docker run cloudflare/cloudflared … --token <TOKEN>».
Соответствующему публичному hostname укажите service `http://app:3000`.

### Переменные окружения

| Переменная                  | По умолчанию        | Описание                                  |
| --------------------------- | ------------------- | ----------------------------------------- |
| `TUNNEL_TOKEN`              | (пусто)             | Токен Cloudflare Tunnel                   |
| `PUBLIC_BASE_URL`           | (пусто)             | Внешний URL портала, напр. `https://...`  |
| `SESSION_SECRET`            | (обязательность)    | Секрет сессий                             |
| `AP_ALLOW_INSECURE_HOSTS`   | loopback            | Разрешённые plain-http хосты для федереции |
| `AP_DEBUG`                  | `0`                 | Подробный лог входящих ActivityPub        |
| `APP_PORT`                  | `3000`              | Порт на хосте (для отладки)               |

`SESSION_SECRET` можно сгенерировать командой `openssl rand -hex 32`.

## Примечание об ассетах

CSS, JS, изображения и аватары из дампов оригинального сайта в репозиторий
не включены (см. `.gitignore`). После чистой установки сайт работает, но без
статических стилей. Положите собственные ассеты в `public/` для полного вида.

## Лицензия

MIT — см. файл [LICENSE](LICENSE).